// @evertrust/db — Drizzle ORM schema + client for the Evertrust ERP Postgres DB.
// Import tables from the `schema` namespace; use `db` for queries.
export * as schema from './schema';
export { db } from './client';
